# ~/.profile: executed by Bourne-compatible login shells.

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

. "$HOME/.local/bin/env"

# uv
export PATH="/root/.local/bin:$PATH"
